[user]
	name = NikoKS
	email = nikolas.kur@gmail.com
[merge]
	tool = nvim
  conflictstyle = diff3
[mergetool]
  keepBackup = false
[mergetool "nvim"]
  cmd = ""
  keepBackup = false
[core]
	excludesFile = ~/.config/git/gitignore
	editor = nvim
  pager = delta
[diff]
  tool = vimdiff
  colorMoved = default
[credential]
	helper = store
[pull]
	rebase = false
[interactive]
  diffFilter = delta --color-only
[delta]
  navigate = true  # use n and N to move between diff sections
  diff-so-fancy = true
  line-numbers = true
  syntax-theme = ansi
  line-numbers-minus-style = red bold
  line-numbers-plus-style = green bold
  line-numbers-zero-style = normal
  minus-emph-style = red 9
  plus-emph-style = green 10
  hunk-header-decoration-style = blue
  hunk-header-style = file syntax
