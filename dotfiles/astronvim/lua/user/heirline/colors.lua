local colors = require("nightfox.palette").load('nordfox')

return {
  git_branch_fg = colors.fg2,
  treesitter_fg = colors.fg2,
  scrollbar = colors.blue.dim,
  insert = colors.blue.bright,
  visual = colors.blue.dim
}
